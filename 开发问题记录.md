# 开发问题记录

> 本文件记录了在本地开发调试过程中遇到的各种问题及解决方案。

---

## 1. IDEA 中 Gradle 构建走 C 盘而非自定义 Gradle 目录

### 现象

`gradle-wrapper.properties` 中配置了 `distributionBase=GRADLE_USER_HOME`，环境变量也已设置了 `GRADLE_USER_HOME=D:\.gradle`，但在 IDEA 中 build 时，`gradle-8.14-bin.zip` 仍然下载到 `C:\Users\zhong\.gradle\wrapper\dists\`，并报超时错误：

```
Timeout of 120000 reached waiting for exclusive access to file:
C:\Users\zhong\.gradle\wrapper\dists\gradle-8.14-bin\...
```

### 原因

IDEA 不会自动读取系统环境变量 `GRADLE_USER_HOME`，它有自己的 Gradle 设置面板。如果该面板中的 `Gradle user home` 字段为空或者指向了默认路径，IDEA 就会使用 `C:\Users\zhong\.gradle\`。

另外，IDEA 的 JVM 进程在你修改环境变量之前就已经启动，即使设置了系统环境变量也不会被 IDEA 识别到。

### 解决

1.需要在本地的环境变量里面设置 `GRADLE_USER_HOME=xxx`才能够正常读取到，对于在用户变量里面的无法读取。


### 原理

命令行用环境变量，IDEA 用 Settings 面板。**两者是独立的，需要在两处分别设置。**

---

## 2. 主程序 vs 测试程序加载哪个配置文件

### 现象

不清楚在 IDEA 中运行 `App.main()` 和运行测试类时，各自加载的是 `application.yml` 还是 `application-test.yml`。

### 原因

- `application.yml` 位于 `src/main/resources/`，在主程序的 classpath 上
- `application-test.yml` 位于 `src/test/resources/`，仅在测试的 classpath 上
- `application-{profile}.yml` 命名规则：只有激活了对应 profile 才会加载

### 总结

| 场景 | 加载的配置 |
|------|-----------|
| IDEA 中运行 `App.main()` | **仅 `application.yml`** |
| `./gradlew bootRun` | **仅 `application.yml`** |
| 带 `@ActiveProfiles("test")` 的测试 | `application.yml` 叠加 `application-test.yml`（后者覆盖前者） |
| 没有 `@ActiveProfiles` 的测试 | 仅 `application.yml` |

---

## 3. Docker 端口映射原理：宿主机 localhost 访问容器服务

### 现象

测试配置 `application-test.yml` 中连接 PostgreSQL、Redis 等中间件时使用的是 `localhost`，而这些服务实际上跑在 Docker 容器里，为什么能通？

### 原因

Docker Compose 中的 `ports` 配置实现了**端口映射（Port Mapping）**：

```yaml
postgres:
  ports:
    - "5432:5432"   # 宿主机 5432 → 容器 5432
redis:
  ports:
    - "6379:6379"   # 宿主机 6379 → 容器 6379
minio:
  ports:
    - "9000:9000"   # 宿主机 9000 → 容器 9000
```

### 原理图

```
宿主机 (Windows)
  └─ localhost:6379 ──────── 端口映射 ──────── 容器内 6379 (Redis)
  └─ localhost:5432 ──────── 端口映射 ──────── 容器内 5432 (PostgreSQL)
```

Docker 容器间通过服务名（如 `postgres`、`redis`）互相访问（Docker 内部 DNS），宿主机通过 `localhost:端口` 访问。

### 关键点

`application-test.yml` 只需要覆盖与默认值不同的配置项（如 datasource 连接信息），其余部分继承自 `application.yml`。

---

## 4. 方法签名变更后单元测试未同步更新导致编译失败

### 现象

`KnowledgeBaseVectorService.similaritySearch()` 方法被加了一个 `minScore` 参数后，旧的测试类 `KnowledgeBaseVectorServiceTest.java` 仍使用 3 个参数调用，编译报错：

```
无法将类 KnowledgeBaseVectorService 中的方法 similaritySearch 应用到给定类型;
需要: String,List<Long>,int,double
找到: String,<空值>,int
```

### 原因

方法签名变更后，旧的单元测试没有同步更新。`RetrievalEvaluationTest.java` 是新写的，所以写对了 4 个参数。

### 解决

将测试类中所有 `similaritySearch(...)` 调用补上第 4 个参数 `0.0`（表示不设相似度阈值，行为与旧版一致）。

**教训：修改方法签名时，必须同步更新所有调用方，包括单元测试。**

---

## 5. 运行单个测试文件时却报其他测试文件的编译错误

### 现象

在 IDEA 中只运行 `RetrievalEvaluationTest`，构建却失败并报告 `KnowledgeBaseVectorServiceTest` 有编译错误：

```
FAILURE: Build failed with an exception.
Execution failed for task ':app:test'.
```

### 原因

**Gradle 的 `test` task 是"全量编译"**，不是按需编译：

```
运行任意测试
  → 编译 src/test/java 下【所有】Java 文件
  → 任一文件编译失败 → 整个 task 失败
  → 目标测试根本没机会运行
```

### 解决

修复所有编译错误后，再运行目标测试。

> 注意：IDEA 内置的测试运行器在单个文件编译失败时可以跳过它去运行目标测试，但通过 Gradle task 方式运行时会被卡住。

---

## 6. Redis 连接失败：Windows Redis 与 Docker Redis 端口冲突

### 现象 1：`RedisAuthRequiredException`

```
Caused by: org.redisson.client.RedisAuthRequiredException
```

### 原因

Windows 宿主机上安装了一个原生 Redis 服务（PID 6140），它占用了 6379 端口，并且配置了 `requirepass` 密码认证。测试程序连接 `localhost:6379` 时实际连到了这个 Windows Redis，被要求密码。

### 现象 2：`Connection refused`

停止 Windows Redis 后，测试仍然报错：

```
Caused by: java.net.ConnectException: Connection refused: getsockopt: localhost/127.0.0.1:6379
```

### 原因

Docker Redis 容器虽然显示 `Running`，但其端口映射没有正确注册到 Windows 的端口转发表。这是 **Docker Desktop WSL2 模式的已知问题**——端口转发偶尔会失效。

### 解决

**第一步：停止 Windows Redis 服务**

```powershell
# 方法 1：停止服务
net stop Redis

# 方法 2：直接杀进程
taskkill /PID 6140 /F
```

**第二步：重建 Docker Redis 容器的端口映射**

```bash
# 方法 1：重启容器（80% 的情况有效）
docker restart interview-redis

# 方法 2：重建容器
docker compose stop redis
docker compose rm -f redis
docker compose up -d redis

# 方法 3：重启整个 Docker Desktop
# 右下角托盘 → Docker 图标 → 右键 → Restart
```

**第三步：验证 Redis 连通性**

```bash
# 确认容器内 Redis 正常运行
docker exec interview-redis redis-cli ping
# 期望输出：PONG

# 确认宿主机端口可访问
netstat -ano | findstr ":6379"
```

### 完整的诊断流程图

```
Connection refused? ────→ 宿主机 6379 端口无人监听
    │                       └→ docker restart interview-redis
    │
RedisAuthRequiredException? → 连到了 Windows Redis（有密码）
    │                         └→ net stop Redis / taskkill
    │
PONG ──────────────────────→ 正常连接
```

---

## 7. Spring Boot 配置中的环境变量占位符

### 配置写法

```yaml
api-key: ${AI_BAILIAN_API_KEY}        # 从环境变量读取，不存在则启动报错
model:   ${AI_MODEL:qwen-plus}        # 从环境变量读取，不存在则用默认值
```

### 解析规则

Spring Boot 的 `${...}` 占位符按以下优先级查找：

1. 命令行参数（`--AI_BAILIAN_API_KEY=xxx`）
2. 系统环境变量（`AI_BAILIAN_API_KEY`）
3. `application.yml` 中定义的属性

### 设置环境变量

```powershell
# PowerShell（当前会话生效）
$env:AI_BAILIAN_API_KEY = "sk-xxxxxx"

# 或者在 IDEA 中设置
# Run → Edit Configurations → Environment variables → AI_BAILIAN_API_KEY=sk-xxxxxx
```

| 写法 | 行为 |
|------|------|
| `${AI_BAILIAN_API_KEY}` | **无默认值**，未设置时启动报错 |
| `${AI_MODEL:qwen-plus}` | **有默认值**，未设置时使用 `qwen-plus` |

### `qwen-plus` 模型说明

`qwen-plus` 是阿里云百炼 DashScope API 的**服务别名**（非开源模型名称），背后对应 Qwen3 系列 Plus 版本。以下是该模型的关键参数：

| 参数 | 值 | 说明 |
|------|-----|------|
| 模型名称 | `qwen-plus` | Qwen3系列Plus模型，实现思考模式和非思考模式的有效融合，可在对话中切换模式 |
| 推理能力 | 显著超过 QwQ | 通用能力显著超过 Qwen2.5-Plus，达到同规模业界SOTA水平 |
| 上下文长度 | **1M (1,000K)** | 最大输入长度 997K，思考模式下最大输入长度 995K |
| 最大输出长度 | 32K | 思考模式下也是 32K |
| 最大思维链长度 | 80K | 思考模式下的思维链上限 |
| RPM | 30,000 | 每分钟请求数 |
| TPM | 5,000,000 | 每分钟 Token 数 |

> 说明：`qwen-plus`、`qwen-max`、`qwen-turbo` 是阿里云百炼的 API 服务别名，背后对应的开源版本会随阿里云策略静默升级。如需使用最新版，可登录百炼控制台查看当前可用模型名称（如 `qwen3-plus`、`qwen3-max` 等）。

---

## 8. Mock 与真实方法签名不匹配的风险

### 注意事项

在 `KnowledgeBaseVectorServiceTest` 中，mock 设置使用的是旧版 API：

```java
when(vectorStore.similaritySearch(query))
```

但实际代码已改为使用 `SearchRequest` 对象调用 `vectorStore.similaritySearch(SearchRequest)`。

这两个是**不同的方法重载**，运行时 mock 可能匹配不了。如果后续运行时测试失败，需要将 mock 改为：

```java
when(vectorStore.similaritySearch(any(SearchRequest.class)))
```

---

## 访问192.168.56.1 /5132前端页面，无法使用upload上传简历的功能

对于本地的前端服务的不需要启动nginx和配置nginx.conf的配置文件（对于本地测试来说，但是对于docker的话需要配置对应的测试文件）

#### 启动前端的服务

```shell
# 本地启动的话，需要先安装node
# 还要开启corepack corepack enable 是用来启用 Node.js 内置的包管理器管理器（Package Manager Manager）。可以自动转化为和项目配置版本需要的依赖
corepack enable

pnpm dev

  VITE v5.4.21  ready in 2592 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.56.1:5173/
  ➜  Network: http://192.168.241.1:5173/
  ➜  Network: http://192.168.112.1:5173/
  ➜  Network: http://10.61.157.96:5173/
  ➜  press h + enter to show help
会出现以下几个路由连接
```

任意点击都是可以跳转前端页面访问的，但是点击任意模块后会有这个路由路径：http://192.168.56.1:5173/api/resumes/upload

使用浏览器访问这个地址的话，发送的是GET请求，不符合当前API下面的POST请求，报`Request method 'GET' is not supported` 异常

**跨域的配置访问不允许**

```yml
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:80}

```

后端 CorsFilter 拿到这个 Origin，去允许列表里找——找不到 192.168.56.1:5173，于是不返回 Access-Control-Allow-Origin 头 → 浏览器预检失败 → 403。



**192.168...** VirtualBox Host-Only Ethernet Adapter 的默认 IP（Windows 端）。不影响功能——Vite 配了 host: '0.0.0.0' 监听所有网卡，proxy 会把 /api 转发到 localhost:8080。（不管是网关还是localhost都是走的localhost:8080端口的）

- **pnpm dev** 启动的是 Vite 提供的临时开发服务器，它**兼职**了静态资源托管和接口代理（Nginx 的部分功能），一切为了本地开发的便捷。
- vite是一个简洁的本地临时服务器，也具备反向代理/路由转接/发送请求等功能
- 生产还是使用nginx，高并发性能好/简洁轻便

---

## 关于上传minio的格式错误和端口错误

software.amazon.awssdk.services.s3.model.S3Exception: S3 API Requests must be made to API port. (Service: S3, Status Code: 400, Request ID: 0)

**这里表示上传错了端口**

S3协议的端口有两个，**9001/9000** **这里的环境变量配置错误了**

1. 9000 = API/服务端口（程序用）
2. 9001 = Console/控制台端口（浏览器管理用）

| 端口     | 角色                   | 谁来访问                       |
| -------- | ---------------------- | ------------------------------ |
| **9000** | API 端口（服务端口）   | 后端程序、S3 SDK 上传/下载文件 |
| **9001** | Console 端口（控制台） | 浏览器打开看 Web 管理界面      |

把 `APP_STORAGE_ENDPOINT` 从 `http://localhost:9001` 改回 `http://localhost:9000`（或直接删掉这个环境变量，让代码用默认值 `http://localhost:9000`），重启后端再上传就好了。

简单记法：**9000 给代码用，9001 给人用**。



## . RAG 检索模块优化全记录

### 背景

RAG 知识库问答模块最初仅依赖 **pgvector 向量相似度检索**，单路召回覆盖不足。目标是通过"四件套"架构（BM25 + pgvector + RRF + Reranker）全面提升检索质量。

### 优化路线

```
阶段 0: 纯向量检索（pgvector cosine similarity）
    ↓
阶段 1: BM25 关键词检索（自建索引 + HanLP 分词）
    ↓
阶段 2: RRF 混合检索融合（BM25 + Vector）           ← 当前已验收
    ↓
阶段 3: Reranker 精排                               ← 待验证
```

---

### 阶段 1：BM25 关键词检索

#### 实现内容

| 模块 | 说明 |
|------|------|
| `BM25Tokenizer` | 基于 HanLP portable 的中文分词器（Spring Bean） |
| `BM25IndexService` | 全量索引重建 / 增量追加，维护 term→document 的倒排索引和文档统计信息，控制读写互斥，存储于 PostgreSQL `bm25_index` 表 |
| `BM25SearchService` | 基于 BM25 公式的全文检索，支持按知识库过滤，基于 JDBC 批量查询（参数化 IN 子句）返回 Top-K 结果 |

#### 技术细节

- **BM25 参数**：k1=1.2, b=0.75（标准默认值，未经调优）
- **分词器**：HanLP portable，轻量级中文分词
- **索引存储**：PostgreSQL `bm25_index` 表，term→docFreq + docId→termFreq 结构
- **并发控制**：`ReadWriteLock` 分离读写，避免重建时检索阻塞

#### 遇到的问题

1. **HanLP 依赖体积过大**：完整版 HanLP 含大量模型文件，最终选择 `hanlp-portable` 轻量版

2. **索引重建耗时**：全量重建约需 30s-1min（取决于 chunk 数量），增量追加可在秒级完成

3. 对于专业的技术名词的默认的HANLP**可能会误拆分导致错误分词的问题，**

   - 这里使用的是让AI统计/分析上传的知识库的方式，总结某一个特殊领域的专业的词汇的方式，并且构建自定义的词汇词典，在做BM25分词的时候，导入自定义的词典并且设置匹配的优先级最高

     （这里之所以要这么设计的话是因为对于HANLP来说，加载了词典只是说有这个词汇能够参与检索，但是词汇还是会拆分在进行检索，比如 **微服务未走高优先级，可能会被拆分“微，服务”，相反就会作为一个完整的词保留**）

     > 一点思考：其实这个只是为了准备测试集使用的能够发挥BM25能够精准定位专业名词的优势，但是自己也问过这样的问题：对于非专业的使用人员来说，他们的领域的专业术语开发人员没有事先拆分和构建词典，那么BM25不是还是会乱拆分词没办法准确定位的吗？
     >
     >  一是不只是依靠BM25的词汇检索，向量检索的话可以匹配相似度高的（看的是向量化之后的数字不是原文本），这个也是混合检索的意义所在，两种检索方式相互配合能够弥补对方的不足
     >
     > 二是对于RAG在某一个特殊的领域的构建还是有现实意义的，提前的分词和构建词典能够让检索更加准确，回答的质量也会更加高

---

### 阶段 2：RRF 混合检索融合

#### 实现内容

| 模块 | 说明 |
|------|------|
| `HybridSearchService` | 同时调用 BM25 + Vector 检索，RRF 融合排序后返回 Top-K 结果 |

#### RRF 公式

```
RRF_score(d) = Σ 1 / (k + rank_i(d))
```
其中 k=60，对 BM25 和 Vector 各自 Top-N 结果进行分数归一化后合并排序。

#### 特点

- **互补性强**：BM25 擅长精确关键词匹配（如类名、配置项），Vector 擅长语义相似检索（同义词、改写）
- **无需训练参数**：RRF 是启发式算法，k 值不敏感，不需要额外训练
- **对两类检索交叉命中时效果最佳**：同一 chunk 同时在 BM25 和 Vector 中出现时，RRF 将其排名推得更靠前

---

### 评测体系建设

#### 评测框架

在 `FullEvaluationTest` 中建立了完整的检索评测流水线：

| 测试方法 | 功能 |
|----------|------|
| `evaluateVector()` | 纯向量检索评测 |
| `evaluateBm25()` | 纯 BM25 检索评测 |
| `evaluateComparison()` | BM25 vs Vector 综合对比 |
| `evaluateHybridSearch()` | RRF 混合检索评测 |
| `diagnoseBm25VectorGap()` | BM25 vs Vector 逐题差异诊断 |

#### 评测指标

- **Recall@5**：Top-5 中命中 ground truth 的比例（主要指标）
- **Precision@5**：Top-5 中 ground truth 的占比
- **NDCG@5**：考虑排序位置的归一化折损累计增益
- **MRR**：第一个命中 ground truth 排名的倒数均值

#### 测试数据集

- **102 道正例题**，覆盖 JUC、JVM、Redis、RocketMQ 四个主题
- 包含直接问答题（Q 编号）和语义改述题（R1/R2/R3 编号）
- 每题有明确的 ground truth chunk ID 列表

---

### 评测结果汇总

#### 整体 Recall@5 对比

| 检索方式 | Recall@5 | 说明 |
|----------|----------|------|
| 纯 BM25 | ~0.81 | 关键词匹配，对改述题弱 |
| 纯 Vector | ~0.88 | 语义匹配，对精确术语弱 |
| **RRF 混合** | **0.90** | 双路融合，全面超越单路 |

#### BM25 vs Vector 差异诊断结果（102 题）

```
Both Pass (BM25√ Vector√):        88  (86%)
BM25 Fails / Vector Passes:         9  (9%)   ← 7% 差距来源
BM25 Passes / Vector Fails:         2  (2%)
Both Fail:                          3  (3%)
```

#### 9 道 BM25 未命中的根因拆解

| 根因类别 | 数量 | 典型题目 |
|----------|------|----------|
| **GT chunk 缺少 query 核心关键词 → 标注/分块问题** | 3 题 | RocketMQ-Q09(CommitLog/ConsumeQueue)、RocketMQ-Q10(同步异步刷盘)、Redis-Q06(ZSet结构) |
| **GT 含关键词但 BM25 排名未进 Top-5 → 排序问题** | 5 题 | RocketMQ-Q11(NameServer高可用)、JVM-Q06(四大引用)、JUC-Q14(computeIfAbsent)、JUC-Q17(Segment)、Redis-Q15(字符串场景) |
| **语义改述型 → BM25 天然劣势** | 1 题 | Redis-R1(宕机恢复) |

#### 3 道 Both Fail 题目

- JUC-R1: "有没有不用互斥锁也能实现线程安全的并发控制方式？"（语义表述）
- Redis-Q16: "ZSet 适合什么场景？"（GT 覆盖不足）
- Redis-R2: "高并发下单线程 Redis 怎么处理几万连接？"（语义表述）

分块边界问题：chunk 8 在"同步需要等待 broker 真正的数据持久化到"断掉，chunk 9 才接上。BM25 搜"同步刷盘"可能落在 chunk 8，但 chunk 8 只有半句话
Chunk 主题混杂：chunk 8 混合了零拷贝 + CommitLog/ConsumeQueue + 刷盘，BM25 的关键词权重被"零拷贝"等无关词稀释
BM25 排序问题（Q11）：内容完全对口但排名没进 Top-5

BM25 的 TF（词频）是按整个 chunk 算的。"NameServer""高可用""宕机"这些词只在 chunk 后半段出现，但 BM25 的分母是整个 chunk 长度——前半段的"刷盘""ACK""生产者线程"这些无关词把关键词密度稀释了。再加上 k1=1.2, b=0.75 的长度归一化，得分就被压下去了。

---


### 新数据集 RRF 评测结果

在原 102 道正例题基础上扩充了 M 编号多 chunk GT 题目（如 M1/M2/M3/M4），所有题目合计 **162 个 ground truth chunk**（非 162 道题），覆盖更复杂的检索场景。

> **指标口径说明**：Recall@5 / Precision@5 / NDCG@5 / MRR 均按**宏平均**计算（每题算一个分数再求平均，简单题和 7-chunk 难题等权重），更反映"平均每道题检索得怎么样"。下面的"chunk级命中"是**微平均**（所有 GT chunk 打平算命中率，chunk 多的题权重更大），两种口径回答不同问题，不可直接比较。

#### 全量指标对比

| 指标 | 纯向量 | 纯 BM25 | RRF 混合 | RRF vs 向量 | RRF vs BM25 |
|------|--------|---------|----------|-------------|-------------|
| Recall@5 | 0.88 | 0.81 | **0.90** | +0.02 | +0.09 |
| Precision@5 | 0.26 | 0.24 | **0.27** | +0.01 | +0.03 |
| NDCG@5 | 0.75 | 0.71 | **0.79** | +0.04 | +0.08 |
| MRR | 0.74 | 0.73 | **0.78** | +0.04 | +0.05 |
| chunk级命中(微平均) | 132/162 | 123/162 | **138/162** | +6 | +15 |

#### 按主题拆分

**RRF 混合检索：**

| 主题 | Recall | Prec | NDCG | MRR | 命中 |
|------|--------|------|------|-----|------|
| JUC | 0.92 | 0.23 | 0.73 | 0.67 | 30/34 |
| JVM | 0.91 | 0.24 | 0.78 | 0.76 | 30/36 |
| Redis | 0.86 | 0.23 | 0.74 | 0.75 | 30/38 |
| RocketMQ | 0.82 | 0.34 | 0.74 | 0.79 | 42/54 |

**纯 BM25 按主题：**

| 主题 | Recall | 说明 |
|------|--------|------|
| JUC | 0.83 | 术语密集，BM25 表现较好 |
| JVM | 0.86 | 同上 |
| Redis | **0.77** | 描述性内容多，BM25 弱 |
| RocketMQ | **0.77** | 同上 |

#### 关键发现

- **RRF 四个指标全面最优**：不是靠扩大召回量碰运气，排序质量（NDCG、MRR）也有实质提升
- **Redis 和 RocketMQ 是 BM25 的弱点**：这两个主题文档偏描述性，精确术语少，恰好是向量检索的强项
- **M 编号多 chunk GT 题目**：GT 分散在多个 chunk 中，RRF 的多路召回能覆盖更广

#### 向量检索失败但 BM25 可能互补的典型案例

| 主题 | 题目 | 问题 |
|------|------|------|
| Redis-Q16 | ZSet 适合什么场景？ | 向量未命中，BM25 也未命中（Both Fail） |
| Redis-M4 | 集群方案解决单点四个问题？ | GT 有 3 个 chunk，向量只命中 1/3 |
| Redis-R2 | 高并发下单线程 Redis 怎么处理几万连接？ | 语义表述，向量也未命中 |
| JUC-R1 | 有没有不用互斥锁也能实现线程安全？ | 语义表述，与 Redis-R2 同类问题 |
| JUC-M1 | synchronized 锁升级全过程？ | GT 3 个 chunk，向量命中 1/3 |
| RocketMQ-M1 | 消息发送到消费完整链路？ | GT 有 7 个 chunk，是覆盖难度最高的题目 |
| RocketMQ-R2 | 双十一高峰期怎么避免流量打崩数据库？ | 业务场景改述，非技术关键词 |

---

### 核心结论

1. **BM25 本身不是瓶颈**：7% 的差距中，真正由 BM25 排序问题造成的仅约 5%（5/102），其余是标注/分块/语义改述等数据问题
2. **混合检索互补性已验证通过**：RRF Recall@5=0.90，四个指标（Recall/Precision/NDCG/MRR）全面超越任一单路，且提升不是靠扩大召回量碰运气，排序质量也有实质改善
3. **RRF vs BM25 提升 9 个点、vs 向量提升 2 个点**：多路融合后新增命中 6 个 chunk（162 个 GT chunk 数据集），主要集中在 M 编号多 chunk GT 题目
4. **标注质量影响显著**：3 道题的 GT chunk 内容与问题不完全对口，说明标注质量对评测结果影响很大
5. **BM25 参数调优意义不大**：当前知识库的词典不具备迁移性，调参换个知识库就无效

---

### 阶段 3：Reranker 精排（已完成 ✅）

#### 实现内容

| 模块 | 说明 |
|------|------|
| `DashScopeRerankService` | 基于阿里云 DashScope `qwen3-rerank` 模型的精排服务，输入 query + 候选文档列表，输出按 `relevance_score` 降序排列的 Top-K 文档 |
| `RerankConfigProperties` | 精排配置类：开关、模型、候选数门槛（`minCandidatesForRerank=8`）、短问题跳过阈值（`shortQueryThreshold=6`）等 |
| `HybridConfigProperties` | 混合检索配置类：召回量（`recallPerPath=30`）、RRF 融合后候选数（`recallTopK=30`）、非精排路径输出条数（`finalTopN=5`） |

#### 三级分层架构

```
Level 3（最高质量）：混合检索 + RRF + Rerank    ← hybrid=true && rerank=true && 场景闸门全通过
Level 2（默认基线）：混合检索 + RRF              ← hybrid=true，但 rerank 关闭/跳过/降级
Level 1（最终兜底）：纯向量检索                  ← hybrid=false
```

#### 技术细节

- **Reranker 模型**：`qwen3-rerank`（阿里云 DashScope，gte-rerank 系列已于 2026-05-30 下线）
- **API 端点**：`https://dashscope.aliyuncs.com/compatible-api/v1/reranks`（与 Chat/Embedding 的 `compatible-mode` 端点不同）
- **请求格式**：扁平结构（`query`/`documents`/`top_n` 在顶层，无 `input` 嵌套）
- **降级策略**：Reranker API 失败/超时不重试，直接降级到 Level 2（RRF 结果前 N 条兜底）
- **场景闸门**：短问题（≤6 字）、纯英文缩写（如"JVM""GC"）、候选数 ≤ 8 时自动跳过 rerank

#### 评测结果

**四路对比（全量 4 主题，宏平均）：**

| 指标 | 纯向量 | BM25 | RRF 混合 | RRF+Rerank | Rerank vs RRF 提升 |
|------|--------|------|----------|------------|-------------------|
| Recall@5 | 0.88 | 0.81 | 0.90 | **0.90** | —（不变，符合预期） |
| Precision@5 | 0.26 | 0.24 | 0.27 | **0.27** | —（不变，符合预期） |
| NDCG@5 | 0.75 | 0.71 | 0.79 | **0.85** | **+0.06** |
| MRR | 0.74 | 0.73 | 0.78 | **0.88** | **+0.09** |

#### 关键发现

1. **Recall 和 Precision 不变是符合预期的**：Reranker 不改变候选集（输入来自 RRF 的 30 条），只重排序。它做的是"精排"而非"扩召回"，Recall 在召回阶段就已锁定
2. **NDCG 和 MRR 显著提升证明了精排价值**：Reranker 的 cross-encoder 架构能深层理解 query 和文档的语义关系，把最相关的文档推到更靠前的位置。NDCG +0.06、MRR +0.09，说明排序质量有实质改善
3. **Reranker 对排序质量敏感的题目提升更明显**：原本 RRF 排名靠后但实际高度相关的 chunk，在 Reranker 精排后被推到 Top-3 甚至 Top-1
4. **Both Fail 的 3 道题未被 Reranker 拉回**：JUC-R1（无锁并发）、Redis-Q16（ZSet 场景）、Redis-R2（高并发连接）在 RRF 30 条候选池中 GT chunk 根本不在，Reranker 只能排已有候选，无法"无中生有"。这是召回瓶颈，需从分块/标注层面解决

---

### 关键技术细节记录

#### Vector 相似度显示为 0.00 的 bug

诊断测试中所有 Vector 结果相似度均显示 `sim=0.00`，原因是 `distance` metadata 的解析方式有误：

```java
// 当前写法（有问题）
Object dist = doc.getMetadata().get("distance");
simScore = dist != null ? 1 - (double) dist : 0;
```

pgvector 的 `similarity_search` 返回的 `distance` 可能是 cosine distance 本身（范围 0~2），也可能是 `1 - cosine_similarity`。需要根据实际 pgvector 算子确认正确的转换公式。不过这不影响排序结果，仅影响诊断日志中的相似度显示。

#### BM25 索引与数据库一致性问题

BM25 索引存储在 PostgreSQL 而非内存中，好处是服务重启不丢索引，但需要确保与 `vector_store` 表的 chunk 数据保持同步。当前的增量追加机制在 chunk 删除场景下需要额外处理（标记删除或重建）。

---

## 多轮对话记忆与上下文压缩实施计划 — 设计问题记录

> 审查 `多轮对话记忆与上下文压缩实施计划.md` 时发现的问题。按"是否会导致功能性故障"分类，只记录必须修复的崩溃级问题，优化级问题暂不处理。

---

### 问题 1：无压缩下限门槛，每轮都触发压缩（API 调用放大 ~30 倍）

#### 现象

`overflowEnd` 和 `startOrder` 每新增一条消息都同步 +1（`overflowEnd = lastOrder - windowSize`，`startOrder = lastEndMsgOrder + 1`），导致 `startOrder <= overflowEnd` **永远为 true**，每新增一条消息都进入压缩逻辑。

#### 崩溃原因

这不是慢，是会**打爆 LLM API 限额**：

- 每新增一条消息触发一次压缩调用，API 调用量放大 ~30 倍（原本期望 30 条压一次）
- `qwen-plus` RPM=30000 看着大，但 30 倍放大 + 多会话并发，实际很容易触顶
- 每次只压 1 条，递归摘要被反复改写，信息漂移风险加剧

#### 修复方向

加下限门槛：`overflowCount < compressBatch` 时不触发，`compressBatch` 同时充当上限封顶和下限门槛。另加 `overflowCount >= windowSize` 强制压缩兜底，防止溢出消息量超过窗口导致读取端出现空洞。

```java
int overflowCount = overflowEnd - startOrder + 1;
if (overflowCount < properties.getCompressBatch()) {
    return;  // 不够一批，攒着
}
```

三层控制逻辑：
- `overflowCount < compressBatch`：攒着不触发（下限门槛）
- `overflowCount >= compressBatch`：正常触发压缩，取前 `compressBatch` 条（上限封顶）
- `overflowCount >= windowSize`：强制触发压缩兜底（防止溢出消息已经超出窗口，读取端出现空洞）

#### 参数含义速查

代码里用到的几个变量名比较抽象，这里集中解释：

| 变量 | 含义 | 计算公式 | 举例（totalMessages=80, windowSize=50, compressBatch=30, 上次压缩到 msg 30） |
|------|------|----------|--------------------------------------------------------------------------------|
| `lastOrder` | 当前会话最新一条消息的序号 | 取消息列表最后一条的 `messageOrder` | `80` |
| `overflowEnd` | 溢出区间的**右边界**（最新窗口的起点之前） | `lastOrder - windowSize` | `80 - 50 = 30`（msg 30 之前都算溢出） |
| `existing.endMsgOrder` | 上次压缩已经压到哪了（摘要覆盖范围的终点） | 从摘要表读 | `30`（假设上次压到 msg 30） |
| `startOrder` | 本次要压缩的溢出区间**左边界** | `existing.endMsgOrder + 1`（首次为 1） | `30 + 1 = 31` |
| `overflowCount` | 还没被压缩的溢出消息数量 | `overflowEnd - startOrder + 1` | `30 - 31 + 1 = 0`（没有新溢出，攒着） |

> **关键认知**：`overflowCount` 指的不是"当前窗口里的消息数"，而是**"窗口之外、还没被压缩的溢出消息数"**。总消息量 = 溢出消息(overflowCount) + 最近窗口(windowSize)。

#### 三层控制各自的作用

| 层级 | 触发条件 | 作用 | 何时生效 |
|------|----------|------|----------|
| 第一层 | `overflowCount < compressBatch` 且 `< windowSize` | **正常攒批**：还没攒够 30 条，不浪费 LLM 调用 | 绝大多数正常轮次 |
| 第二层 | `overflowCount >= compressBatch` | **正常触发**：攒够 30 条了，压一批 | 每 30 条溢出触发一次 |
| 第三层 | `overflowCount >= windowSize` | **强制兜底**：溢出已经堆到 50 条，必须重试 | 仅当第二层持续失败时生效 |

**第三层为什么阈值是 `windowSize` 而不是 `compressBatch`？**

因为 `windowSize` 是读取端能容忍的溢出上限。读取端取的是 `[endMsgOrder+1, 最新]`，这段长度 = `overflowCount + windowSize`：
- `overflowCount < windowSize` 时，读取端最多取 `2 × windowSize ≈ 100` 条，token 还在安全区（66K / 1M = 6.6%）
- `overflowCount >= windowSize` 时，读取端取的消息量超过 2 倍窗口，开始有失控风险

**在 LLM 100% 成功的理想情况下，第三层确实是多余的**，第二层就够了。第三层是为应对第二层失败（LLM 接口不可用、压缩抛异常等）时不让系统静默失控而存在的保险——当溢出量已经堆到危险线，必须强制重试，不能再因"下限门槛没到"或"上次失败了"而继续攒着。

---

### 问题 2：读取端固定条数截断与压缩进度脱节（消息丢失）

#### 现象

读取端 `getStreamAnswer` 按固定 `windowSize` 条取消息（`findRecentBySessionId(sessionId, windowSize)`），压缩端按 `compressBatch` 门槛触发。两套独立逻辑会脱节。

#### 崩溃原因

加了问题 1 的下限门槛修复后，压缩不会在 `overflowCount` 达到 `compressBatch` 之前触发。但读取端仍按固定 50 条取——如果总消息数超过 50，第 1~N 条消息会同时被两套逻辑遗漏：压缩不压（不够门槛），读取不读（超出窗口）→ 直接丢失。

**场景推演**（`windowSize=50, compressBatch=30`）：

```
第 60 条：totalMessages=60，因加了门槛 overflowCount=10 < 30，不触发压缩
         读取端 findRecent(50) → 取 msg 11~60
         第 1~10 条消息：压缩不压（不够门槛），读取不读（超出窗口）→ 丢失！

第 61 条：totalMessages=61，overflowCount=11 < 30，仍不触发压缩
         读取端 findRecent(50) → 取 msg 12~61
         第 1~11 条消息：丢失！
```

用户问"我前面说的 XXX"模型答不上来，这是功能性数据丢失。

#### 修复方向

**核心思路**：读取端应从 `endMsgOrder + 1` 开始取消息，跟着压缩进度走，而不是按固定条数截断。这样读取端和压缩端共用同一个进度锚点（`endMsgOrder`），不会出现覆盖空洞。

```java
// 修复后：从上次压缩结束位置 + 1 开始取
ConversationSummaryEntity existing = summaryRepository
    .findBySourceAndSessionId(source, sessionId).orElse(null);
int readFromOrder = (existing != null) ? existing.getEndMsgOrder() + 1 : 1;

List<RagChatMessageEntity> recentMessages = messageRepository
    .findBySessionIdAndMessageOrderAfter(sessionId, readFromOrder);
```

读取端跟随压缩进度后，实际读取范围在最坏情况下可达 `windowSize(溢出) + windowSize(窗口) ≈ 100` 条，对应约 66K token（占 1M 上下文的 6.6%），仍在安全区。

---

### 联动关系

问题 1（无下限）+ 问题 2（读取固定条数）叠加：压缩每轮触发但只压 1 条 → 压缩进度极其缓慢 → 读取端窗口覆盖不到中间消息 → 大量消息丢失，同时 API 调用爆炸。

**修复顺序**：先修问题 1（加下限门槛 + 强制兜底），再修问题 2（读取端跟着压缩进度走）。

---

### 已知优化项（暂不修复）

以下问题不修也不会导致功能故障，仅影响性能/文档/鲁棒性，暂缓：

- **压缩判断每轮查全量历史**：性能问题，结果正确，会话量大时再优化
- **`compressBatch < windowSize` 缺启动校验**：默认值 30 < 50 正确，参数调反才会出问题
- **压缩持续失败静默降级**：故意接受的降级行为，问答主流程不受影响
- **`lastOrder` 获取方式**：属上述全量查询优化的实现细节

---

## 多轮对话记忆与上下文压缩实施计划 — 三次审查问题与修复方案

> 2026-07-18 三次审查 `多轮对话记忆与上下文压缩实施计划.md` 时，结合现有代码（`RagChatSessionService`、`RagChatMessageRepository`、`RagChatMessageEntity`、`KnowledgeBaseQueryService`、`AgentAsyncConfig`）逐条比对，发现 11 个问题，已全部在计划文档中同步修复。本节记录问题与修复方案，便于追溯。

### P0 崩溃级（会导致功能性故障）

#### 问题 8：`messageOrder` 从 0 开始，但计划假设从 1 开始

**现状**：`RagChatSessionService` 第 113-120 行：

```java
int nextOrder = session.getMessageCount();  // 初始 0
userMessage.setMessageOrder(nextOrder);     // 首条 order=0
```

`messageOrder` 从 **0** 开始。但计划 §5.1 `readFromOrder` 无摘要时默认值写的是 1，§4.7 注释里"msg 30 之前都算溢出"也是按从 1 开始举例。

**影响**：首轮对话的摘要会从 order=1 开始，**order=0 的首条消息永远读不到**；读取端首次查询也会跳过 order=0，造成首条消息丢失。

**修复**：`readFromOrder` 无摘要时默认值改为 **0**；所有 order 注释和举例对齐"从 0 开始"语义。修复位置：§5.1 `getStreamAnswer`、§4.7 `compressIfNeeded` 注释。

---

#### 问题 9：`@Async` 无自定义线程池，会用 `SimpleAsyncTaskExecutor`

**现状**：`AgentAsyncConfig` 只加了 `@EnableAsync`，项目里搜不到任何 `TaskExecutor` / `ThreadPoolTaskExecutor` 定义。Spring Boot 在没有自定义 `TaskExecutor` 时，`@Async` 默认用 `SimpleAsyncTaskExecutor`——**每次调用都新建一个线程，不复用，没有队列，没有上限**。

**影响**：压缩任务是调 LLM 的重操作（可能阻塞数十秒），多会话同时触发压缩时，`SimpleAsyncTaskExecutor` 会无限制创建线程，每个线程阻塞在 HTTP 调用上，**直接把 JVM 线程数打爆**。CODEBUDDY.md 提到项目其他异步任务都用 Redis Stream + `newSingleThreadExecutor` 单线程消费，唯独这里的 `@Async` 是裸奔的。

**修复**：`AgentAsyncConfig` 新增 `memoryCompressExecutor` Bean（核心 2、最大 4、队列 50、`CallerRunsPolicy` 兜底）；`triggerCompress` 的 `@Async` 改为 `@Async("memoryCompressExecutor")`。修复位置：§4.8 `AgentAsyncConfig`、§4.7 `triggerCompress`。

---

#### 问题 10：`count` 与 `order` 语义混用，差 1

**现状**：`completeStreamMessage` 用 `countBySessionId` 拿 `totalMessages`（数据库行数），`compressIfNeeded` 用 `messages.get(last).order()` 取 `lastOrder`（最大 order）。order 从 0 开始时，第 N 条消息 order=N-1，count=N，**两者差 1**。

**影响**：计划里用 `totalMessages > triggerThreshold(60)` 触发，同时用 `lastOrder - windowSize` 算溢出边界，count 和 order 混用导致边界条件出错。

**修复**：统一用 order 值做判断——新增 `findMaxOrderBySessionId`，`totalMessages = lastOrder + 1`；废弃 `countBySessionId` 在压缩场景的使用。修复位置：§5.1 `completeStreamMessage`、`RagChatMessageRepository`。

---

### P1 设计缺陷（埋坑，高并发下暴露）

#### 问题 11：`@Async` + `@Transactional` 并发下乐观锁静默失败

**现状**：`@Async` 和 `@Transactional` 同时用在 `triggerCompress` 上，执行顺序是先开新线程再在新线程开事务。`compressIfNeeded`（调用方事务）计算 `startOrder` 后传给 `triggerCompress`（异步新线程），但 `triggerCompress` 内部又重新查了一次 `existing`。两次查询 `existing` 之间有时间差，如果期间另一个请求触发了压缩并更新了 `endMsgOrder`，乐观锁冲突会抛 `OptimisticLockException`，而原计划 catch 只记日志不重试。

**影响**：高并发会话下压缩**静默失败**，摘要永远不更新，最终消息越来越多撑爆上下文。

**修复**：`triggerCompress` catch `OptimisticLockingFailureException` 做一次重试，覆盖绝大多数并发场景。修复位置：§4.7 `triggerCompress`。

---

#### 问题 12：`findBySessionIdAndMessageOrderAfter` 未声明

**现状**：计划 §5.1 调用了 `messageRepository.findBySessionIdAndMessageOrderAfter(sessionId, readFromOrder)`，但现有 `RagChatMessageRepository` 没有这个方法，计划也没在 Repository 改造部分提到要新增。

**影响**：编译失败。

**修复**：Repository 补充三个方法声明：
- `findBySessionIdAndMessageOrderAfter`（读取端跟随压缩进度）
- `findBySessionIdAndMessageOrderBetween`（`completeStreamMessage` 范围查）
- `findMaxOrderBySessionId`（替代 `countBySessionId`）

修复位置：§5.1 Repository 改造。

---

#### 问题 13：`completeStreamMessage` 全量查消息

**现状**：计划 §5.1 改造 `completeStreamMessage` 时，查全部消息再 stream map 成 `CompressibleMessage`：

```java
List<RagChatMessageEntity> allMessages = messageRepository
    .findBySessionIdOrderByMessageOrderAsc(sessionId);
```

**影响**：每次流式回答完成后都查全量消息，对话越长查询越慢，60+ 轮时每次查 60+ 行，且在 SSE 回调线程做同步操作。

**修复**：改为按 `[lastOrder - 2*windowSize, lastOrder]` 范围查，只取判断所需的消息子集（`compressBatch < windowSize`，2*windowSize 足够覆盖溢出区间）。修复位置：§5.1 `completeStreamMessage`。

---

### P2 代码质量（不阻塞功能，但影响可维护性）

#### 问题 14：`findRecentBySessionId` 是死代码

**现状**：问题记录表问题 1 原标"暂缓"，但问题 7 修复后读取端改为从 `endMsgOrder + 1` 开始取，`findRecentBySessionId`（DESC + LIMIT 固定条数截断）这套方案**已被废弃**。然而 §5.1 仍保留着它的完整定义和详细注释，会让人误以为还需要实现。

**修复**：从 Repository 删除 `findRecentBySessionId`，并在问题记录表问题 1 标注"已废弃，被问题 7 替代"。修复位置：§5.1 Repository、§十 问题记录表。

---

#### 问题 15：`deleteSession` 摘要清理顺序问题

**现状**：`deleteSession` 把 `memoryService.deleteBySourceAndSessionId` 放在"现有删除逻辑"之后。现有 `deleteSession` 是先 `existsById` 再 `deleteById`，会话不存在直接抛异常。

**影响**：会话不存在时摘要不会被清理，残留脏数据。

**修复**：调整顺序——**先清摘要再删会话**，保证即使会话不存在也能清理残留摘要。修复位置：§5.1 `deleteSession`。

---

#### 问题 16：缺 `@Value` import

**现状**：§4.7 `MemoryCompressService` 构造函数用了 `@Value` 注解，但 import 列表只有 `@Autowired` 和 `@Lazy`，缺 `org.springframework.beans.factory.annotation.Value`。

**影响**：编译错误。

**修复**：补充 import。修复位置：§4.7 import 列表。

---

### P3 健壮性增强

#### 问题 17：`AgentMemoryProperties` 参数约束无校验

**现状**：`windowSize=50`、`compressBatch=30`、`triggerThreshold=60` 三者有依赖关系（`compressBatch <= windowSize`、`triggerThreshold > windowSize`），但代码里无校验。

**影响**：误配置（如把 `windowSize` 改成 20 但忘改 `triggerThreshold`）会导致压缩行为异常。

**修复**：加 `@PostConstruct validate()` 校验约束关系，违例抛 `IllegalStateException`。修复位置：§4.6 `AgentMemoryProperties`。

---

### 修复联动关系

```
问题 8（order 从 0 开始）
   └→ 修正 readFromOrder 默认值（0）+ startOrder 注释
   └→ 引出问题 10（count 与 order 差 1）→ 改用 findMaxOrderBySessionId

问题 9（线程池）→ 新增 memoryCompressExecutor + @Async 指定 bean 名

问题 11（乐观锁）→ 配合问题 9 的线程池改造，在 triggerCompress 加重试

问题 13（全量查询）→ 配合问题 10 的 order 统一，改范围查询

问题 12（缺方法声明）→ 问题 8/13 的修复依赖新 Repository 方法
```

> 所有代码改动已同步到 `多轮对话记忆与上下文压缩实施计划.md` 对应章节，无需额外补丁。
